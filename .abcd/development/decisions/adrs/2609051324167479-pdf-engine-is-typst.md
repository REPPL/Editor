---
id: adr-2609051324167479
slug: pdf-engine-is-typst
status: accepted
date: 2026-09-05
supersedes: null
superseded_by: null
related_intents: []
related_rfcs: []
related_adrs: []
---

# ADR-2609051324167479: The PDF is rendered by Typst in the publish pipeline in a journal layout

## Context

The maintainer first wanted a Tufte PDF, then decided against Tufte for print in favour of a modern academic-journal layout with strong tables and numbered references. The PDF is produced in the pipeline, never in the app. Candidates were Pandoc with LaTeX, a headless browser printing the article, and Typst.

## Decision

The pipeline renders the PDF with Typst from the same Markdown source, through Pandoc's Typst writer or a direct converter, using one built-in journal-style template. Citations resolve from the same BibTeX file. Page-break comments in the source are honoured; slide-only constructs are ignored. Each variant renders to its own PDF, stored beside the version and linked from the presenter.

## Alternatives Considered

1. Pandoc plus LaTeX with a journal class. The canonical route with the most templates, at the cost of a TeX toolchain in CI and slower builds. Rejected by the maintainer in favour of the lighter toolchain.
2. A headless browser printing the article with a print stylesheet. One renderer for web and print, but footnotes, page breaks, and tables are weaker. Rejected.

## Consequences

CI needs Typst and Pandoc, both single binaries. The journal template is Editor's own or one of Typst's published ones; that choice is open in the brief's evidence chapter. Dropping Tufte from print removes the margin-note problem entirely from the PDF path.
