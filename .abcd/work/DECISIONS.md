# DECISIONS

Append-only, one dated line per decision. Architecture-shaping decisions
graduate to an ADR under `../development/decisions/adrs/`.

- 2026-09-05 — Adopted the three-tier `.abcd/` layout, an `AGENTS.md` router with `CLAUDE.md` as a symlink, and committed name-guard hooks under `.githooks/`.
- 2026-09-05 — Canonical identity recorded in `.abcd/development/IDENTITY.md`; README and AGENTS.md openings render from it.
- 2026-09-05 — Opted in to AI disclosure on commits via `.githooks/prepare-commit-msg`.
- 2026-09-05 — The record tiers (`.abcd/development/`, `.abcd/work/`) are committed even though the repo is public; only `.abcd/.work.local/` is ignored.
- 2026-09-05 — First product intent filed as itd-2609051221553568 (draft): HTML-frontend Markdown editor with drag-and-drop assets and a responsive HTML presentation for desktop, tablet, and phone. Hosting shell (Tauri versus browser app) left open pending an ADR.
- 2026-09-05 — The editor is a desktop app; the presentation output is a static website optimised for desktop screens, iPad, and iPhone. Desktop shell (Tauri is the candidate) still needs an ADR.
- 2026-09-05 — Product story fixed as: create on the desktop, edit text on a tablet, present anywhere on the web. Tablet editing assumed to go through a local-network server in the desktop app, no cloud; recorded as an open question on itd-2609051221553568.
- 2026-09-05 — Document model is a book hierarchy: Part = folder, Chapter = Markdown file, Section = H2, Sub-section = H3, ordered by numeric prefix. Supersedes the single-file horizontal-rule model.
- 2026-09-05 — Three exports from one source: website (videos, interactive elements), PDF (print), web slides (Section per slide, Sub-section per step). All optimised for desktop, iPad, iPhone.
- 2026-09-05 — Full Emacs key bindings are the editing binding set, on desktop and on the tablet view with a hardware keyboard. The binding table is to be written into the spec.
- 2026-09-05 — Editor uses no network: the local-network server for tablet editing is dropped. Tablet editing path reopened as a question on itd-2609051221553568.
- 2026-09-05 — Web exports are packaged two ways at the author's choice: one self-contained HTML file with everything embedded (videos optionally external), or an asset folder, optionally zipped, that uploads unchanged to a public web page.
- 2026-09-05 — Publishing: Editor pushes to a GitHub repository served by Cloudflare Pages. One public reader page; each document in a folder named by an unguessable hash, loaded when the reader URL carries the key. Unlisted, not private. Supersedes the folder/zip packaging.
- 2026-09-05 — The single self-contained HTML file is both reader and editor: it imports and exports Markdown only. It is the tablet and anywhere-else editing path; assets are added only in the desktop app.
- 2026-09-05 — Publishing splits into two channels: public code (reader, slides engine, site shell) in GitHub served by Cloudflare Pages; private content (Markdown and assets) uploaded straight to Cloudflare storage under a hash, never through GitHub.
- 2026-09-05 — The PDF is rendered by the publish pipeline and stored beside the document; the desktop app does not export PDF.
- 2026-09-05 — Publishing simplified to one production system: Editor builds, commits under a hash folder, pushes to the production GitHub repository, and returns the hash; Cloudflare Pages deploys a public presenter page plus every document. Supersedes the two-channel design. Repository visibility assumed private, to confirm.
- 2026-09-05 — Assets reachable from anywhere for editing away from the computer is parked as a future feature.
- 2026-09-05 — The maintainer's prototype online article (Org-mode export, Tufte layout, reader controls, easter eggs with Konami reveal) is the reference reading experience; recorded in research/notes/2026-09-05-prototype-article.md. The public presenter domain is magnumesque.com.
- 2026-09-05 — Three exports from one text: Tufte-style online article, Tufte-style PDF, and slides, all using footnotes and native bibliography references from a bibliography file beside the document. Citation syntax assumed Pandoc `[@key]`, to confirm.
- 2026-09-05 — Prototypes are recorded by form only: never their topic, location, or content. The slide prototype's conventions (rule breaks, column splits, layout and source comments, speaker notes) are the reference for slide authoring; slides are authored in the same text, not only derived from the hierarchy.
- 2026-09-05 — An existing personal writing project is the acceptance test for Editor, recorded form-only after three independent model reviews. It adds to the design: split-on-import and a fourth heading level, variants as an in-Markdown marker, referenced-not-copied large assets, a `Video:` fallback line, reader-layer annotations with round-trip conventions, and byte-fidelity on round trip.
- 2026-09-05 — Product grill, round one. Audience: the maintainer first, others later. Article is the rendering to perfect first; slides for a specific talk are the first usable slice. The app is the editor with Emacs bindings ported. Variants are core, selected by link, no switcher. Reader annotations are sidecar files per chapter, private by default, publishable by the author as a public layer. Publishing is per document unlisted or gated.
- 2026-09-05 — Product grill, round two. Markdown canon is Pandoc-compatible plus fenced divs. On disk: folder of chapters with split on import and four heading levels. Shell: Tauri 2. PDF: not Tufte; a modern journal layout rendered by Typst in the pipeline. Hash: stable document id plus versioned content hashes. Assets: embed small, link large, threshold set by the author. Slides: hybrid, hierarchy by default with authored breaks inside. Recorded in brief/02-constraints.md.
- 2026-09-05 — Slides: Section = horizontal slide, Sub-section = vertical slide beneath it, each one's prose its speaker notes; a Sub-sub-section folds into its Sub-section's notes. Supersedes "Sub-section = step" in brief/02-constraints.md.
- 2026-09-05 — Document metadata lives in `document.yaml` at the document root — title, subtitle, abstract, author, affiliation, variants, default variant, bibliography, citation style, theme, asset threshold, and the stable id. A chapter's front matter is chapter-level only. Closes the open question and corrects both ADRs that said "front matter".
- 2026-09-05 — Assets above the deploying host's per-file ceiling (25 MiB, to verify) are uploaded to object storage under the document's id by the publish pipeline, never through git; the manifest records the published URL. Editor holds no storage credential. One publish action, two stores behind it.
- 2026-09-05 — Gated documents carry a committed per-document access file in the production repository, never in the document folder; the pipeline applies the Cloudflare Access policy from it with its own credential. The gate is per document id and covers every version and variant; what a version says never changes, but who may open it follows the document.
- 2026-09-05 — Each variant gets an unguessable path token minted with the id: the stable link is `/<id>/<token>/` and a version is `/<id>/<token>/v/<hash>/`. An id alone renders the empty shell, so no variant's link can be shortened or guessed into another's.
- 2026-09-05 — "The presenter reveals nothing" means the shell, a wrong id, and a missing id render identically — the site's not-found page is the shell — and nothing is listed. It is not a claim about response codes.
- 2026-09-05 — The "no machine in the document" discipline narrows to the author's machine: absolute local paths, local hostnames, usernames, machine-local settings. Published links and gated media URLs are permitted references.
- 2026-09-05 — "Nothing is stored about a reader" binds Editor and the pages it publishes; the access provider necessarily holds the identity of a reader who signs in to a gated document, and the discipline states that carve-out.
- 2026-09-05 — "Network only on publish" means the app touches the network only during a publish Alice started — push, upload, applying access, and following that publish's pipeline until it reports done or failed — and at no other time.
- 2026-09-05 — Device widths fixed at iPhone 390, iPad 820, and desktop 1280 CSS pixels, measured in the current Safari and Chromium engines; the legibility discipline holds them and every spec cites them.
- 2026-09-05 — Citations resolve once: the core's print renderer emits Typst source with references already resolved, and Typst typesets and resolves nothing. Pandoc leaves the pipeline; the core emits Typst source directly.
- 2026-09-05 — Import by split writes one Part named for the document, one chapter per level-one heading with a slug rule and numeric prefixes, any preamble as the first chapter, `document.yaml` minted from the front matter or the first heading, and the source manuscript left untouched.
- 2026-09-05 — Drop is disambiguated by target, stated once in brief/05-internals.md: a Markdown file on a Part becomes a chapter, any file on the text becomes a reference at the cursor, a manuscript arrives through Import, and an edited chapter returns through Re-import, matched by path and an export-time hash.
- 2026-09-05 — Conversion of phone-native images belongs to the drop moment at any size and the original is not kept; the threshold, referenced mode, roots, the manifest, and the upload on publish belong to the referenced-assets intent.
- 2026-09-05 — The insert palette is divider, columns, speaker notes, callout, margin aside, credit, variant block, variant span, video block, citation, footnote, page break, easter egg, opening quotation. The callout and the margin aside are the article intent's.
- 2026-09-05 — A variant block may contain headings, so a Section or Sub-section can belong to one variant alone; other variants omit it from the page, the contents, and the deck with no trace.
- 2026-09-05 — Four moments leave plumbing and become intents — moving through the article by keyboard, setting the threshold and naming an asset root, taking a document off the site, and starting a new document — and a tenth discipline, "Orphan, never guess", binds from phase 7.
- 2026-09-05 — Saving is explicit on the save chord; the modeline marks an unsaved buffer; closing or switching chapter with unsaved changes asks, and an external change to a chapter held unsaved is reported rather than resolved silently.
- 2026-09-05 — The publish log records every publish, including a republish of unchanged content, whose entry points at the hash already on the site and adds no version path.
- 2026-09-05 — The reader controls and the once-only quotation travel into the single HTML file and persist per browser in storage scoped to that file; where storage is unavailable they simply do not persist.
- 2026-09-05 — The article is one page per document, with a contents list covering Parts, Chapters, Sections, and Sub-sections; the app's sidebar shows all five levels.
- 2026-09-05 — An easter-egg marker with no block renders as ordinary text, a block with no marker is left out, both are listed as unresolved, and the opening quotation belongs to the first block of the first chapter alone.
- 2026-09-05 — The one-source discipline forbids a rendering being an input rather than any output being one: exported Markdown and annotation files are inputs by design.
- 2026-09-05 — The canon's permitted forms are five — fenced divs with attributes, heading attributes, image attributes, bracketed span attributes, and HTML comments — with Pandoc's own citations and footnotes not being extensions at all; the degrade discipline forbids content wired by filename convention, not ordering by numeric prefix.
- 2026-09-05 — Severity set honestly on the record: `major` on variant fidelity and on the variants, referenced-assets, per-variant-links, and gated-publishing intents; the rest stay minor.
- 2026-09-05 — The product's own public domain is identity rather than infrastructure, and AGENTS.md's privacy rule now says so.
- 2026-09-05 — The deck carries a credit line at the foot of each slide and no reference list; the article and the PDF carry the generated lists, and a slide's credited key is always one of their entries.
- 2026-09-05 — Restored to the record: video URL and other-file drops, deck input by swipe, tap, and arrow keys, PDF tables and placed images, the named browser engines, conversion originals not kept, and the sidebar's asset count. Video transcripts and full-text search become open questions for later intents; the browse-and-edit-mode split is recorded as an alternative rejected.
- 2026-09-05 — Schemas stay in the internals chapter, which gains a `label` field on annotation files, a `layers/` folder for files received from readers, and the export-time hash that makes re-import conflicts detectable; intents test observable behaviour instead.
- 2026-09-05 — Each ADR's `related_intents` now lists the intents that depend on it.
- 2026-09-05 — Canon settled from the worked examples: an image-only paragraph starts a slide unless it is the first block after a heading; `{.divider}` is allowed on a chapter's H1; a two-line title is one heading; a notes div attaches to the slide whose content it follows; image size by `{width="75%"}`; cross-references by `{#fig:...}`; pipe tables with `: Caption {#id}`; one `assets/` per Part; a `{.refs}` heading for an appendix of sources.
- 2026-09-05 — Answers chosen where the brief was silent: the published deck holds the whole document while Present starts at the current chapter; a chapter title opens the deck; prose under a divider heading becomes its notes; a rule may split anywhere below the chapter title; a footnote prints at the foot of its slide.
- 2026-09-05 — Answers chosen where the brief was silent: video sources are tried at read time and fall through on a load failure or a sign-in page; the poster is the author's, else the manifest's, else a bare link; a `.video` block names a referenced asset as `asset:<id>`.
- 2026-09-05 — Answers chosen where the brief was silent: a variant name the metadata does not declare leaves its block out of every rendering and is listed in the sidebar; removing an inline span collapses only the double space it would leave.
- 2026-09-05 — Answers chosen where the brief was silent: the single HTML file carries one variant, reads the whole document, edits one chapter at a time, and holds no annotation surface; getting it to a tablet and back is by whatever means the author already uses.
- 2026-09-05 — Answers chosen where the brief was silent: Alice annotates in the app's article preview; a reader's marks key to the document and its chapter paths and export as one file that is also an import; layers are named, off until turned on, and several can be shown at once; a rehearsal score lasts the sitting.
- 2026-09-05 — Answers chosen where the brief was silent: publish reports which step failed and leaves the deployed site as it was; the presenter, the article script, and the slide engine are maintained once at the site root, which closes that open question; a chapter always sits inside a Part.
- 2026-09-05 — Above-ceiling assets are uploaded by the app straight to object storage during a publish Alice started, with a write-only upload token scoped to the store and kept in the system keychain: the one credential Editor holds. The pipeline verifies each object's hash against the manifest before deploy. Closes the open question of how large bytes reach a pipeline that cannot receive them through git.
- 2026-09-05 — Pandoc is not part of the publish pipeline: the core emits Typst source with references already resolved. Pandoc remains a development convenience for converting source documents into examples.
- 2026-09-05 — The video canon gains a fourth role, `remote:`, for a plain public URL beside `local`, `site`, and `gated`; a dropped or pasted public video URL writes it. Recorded in brief/05-internals.md section 3 and carried by `src/core/tree.ts`.
- 2026-09-05 — When conversion of a phone-native image is unavailable, the drop is refused: nothing is written into the assets folder, nothing into the chapter, and the modeline names the file. No placeholder, because a reference to a picture that is not the picture is a quiet lie inside a portable document, and leaving the original is what the intent forbids. Closes that open question in brief/03-evidence.md.
- 2026-09-05 — Phone-native decoding goes through Apple's ImageIO (objc2-image-io 0.3.2), not a Rust crate: `image` 0.25 excludes HEIF on patent grounds, `libheif-rs` needs a Homebrew libheif and puts an LGPL library in an MIT bundle, and the one pure-Rust decoder is AGPL-only. `sips -s format jpeg` is the recorded fallback.
- 2026-09-05 — EXIF is stripped from every copied image. A JPEG carrying EXIF, IPTC or a comment segment is re-encoded through ImageIO; a PNG or WebP loses only its metadata chunks, losslessly, because turning a lossless image lossy to strip a text chunk costs the author more than it saves them. Every conversion is swept the same way afterwards, since ImageIO writes an EXIF block of its own.
- 2026-09-05 — A dropped file is named to the shell by a nonce, never by a path: Rust receives the native `WindowEvent::DragDrop`, holds the paths for thirty seconds under a nonce, and emits `document://dropped` with the nonce and the drop point in logical pixels. `drop_on_chapter(chapter, nonce)` refuses a nonce it did not mint or one that has expired.
- 2026-09-05 — Above the threshold a dropped file is recorded as a referenced asset naming a root rather than a path — the source's own folder name, or `dropped` where that name would be the author's home folder or account name — and the text carries `asset:<id>`. Resolving the root belongs to map #27; until then the asset is unresolved, which is what brief/05-internals.md section 6 says an unnamed root means.

- 2026-09-05 — The default mapping's image exception is generalised from "the first block after a heading" to "the first block of the slide in force", so a picture that opens a rule's continuation shares that slide instead of making an empty one beside it; a divider slide never takes it, because its face is the headline alone.
- 2026-09-05 — An image slide inherits the face-or-notes placement of the slide it split from, so blocks after a picture on a rule's continuation stay on the face and blocks after a picture under a heading go to the notes. Without it the bullets under the second picture of a real chapter kept for local testing would leave the audience's slide.
- 2026-09-05 — The deck runs reveal.js with `disableLayout: true` over Editor's own `src/core/render/slides.css` and loads no reveal theme: the theme fixes a font size, and a slide that reflows at the reader's width is what "legible on three device classes" costs. Exact visual placement is not available, which `03-evidence.md` already records as the price paid.
- 2026-09-05 — Inline content is rendered by the render layer, not by the parse: `src/core/render/html.ts` holds the escaping, the inline nodes, and the block shapes a deck and an article share, and each renderer reads its own column of `canon.ts` over it.
- 2026-09-05 — The stable id, each variant token, and the version hash are sixteen bytes written as RFC 4648 base32, lower-cased and unpadded: twenty-six characters over `[a-z2-7]`, one grammar for every published path segment. Base32 rather than hex because the production repository is checked out on a case-insensitive file system; base32 rather than base64url because the alphabet omits `0`, `1`, `8` and `9`. The brief's illustrative sixty-four-bit `id:` in 05-internals.md section 1 is corrected and gains `variant_tokens:`, and section 9's open question about the three lengths is struck.
- 2026-09-05 — The article skeleton is built in phase 1 rather than waiting for its own intent: the deck bundle's `src/core/render/article.ts` emits it unstyled, the publish build wraps it as the version folder's `index.html` with a minimal legible stylesheet, and intent 9's later spec inherits the file rather than starting one. One parse, two renderings, from the first slice.
- 2026-09-05 — The key log built for the Emacs-chord spike is kept in the shipped app behind `C-h k` rather than being thrown away with the spike: it is the only way an author can see what a chord reached, and it is what a bug report about a keyboard is made of.
- 2026-09-05 — Raw HTML in a chapter is escaped by every rendering and kept verbatim by the parse. A chapter is a file and a file can come from anywhere — a dropped Markdown file becomes a chapter — so the tree keeps what the author wrote, because a plain tool reads the same file, and no renderer ever hands it to a browser as markup. Recorded as a `Raw HTML` row in brief/05-internals.md section 3 and in `src/core/canon.ts`.
- 2026-09-05 — Speaker notes are a split view inside the present window, opened with `s`, not reveal.js's own speaker popup. That popup calls `window.open("about:blank")` and writes an inline-script document into it; an `about:blank` popup inherits its opener's content policy, and the present window's is `script-src 'self'`, so the script never runs. Relaxing the policy would relax it for the window that renders the author's own Markdown, which is the one window that must not be relaxed.
- 2026-09-05 — A copied JPEG is never re-encoded. EXIF, IPTC and comment segments are stripped losslessly from the byte stream, so the compressed scan reaches the assets folder exactly as it left the camera; re-encoding survives only for a phone-native conversion, where it is unavoidable. This replaces the earlier rule that re-encoded a copied JPEG carrying metadata.
- 2026-09-05 — `serde_yml` is replaced by `serde_yaml_ng` 0.10.0, pinned. `serde_yml` is a fork of the archived `serde_yaml` and carries RUSTSEC-2025-0068 and RUSTSEC-2025-0067 through its `libyml` backend, at the boundary where the shell reads an untrusted `document.yaml`. `cargo audit` now runs as part of `npm run lint` and reports zero vulnerabilities.
- 2026-09-05 — A construct's phase has one source: the `phase` column of `src/core/canon.ts`. `src/core/inserts.ts` names its canon row and reads the number from it, so the palette cannot offer a construct in a phase the placement table has not seeded, and a later phase changes one table rather than two.
- 2026-09-05 — Front matter follows Pandoc's rule with one tightening: the opening `---` may not be followed by a blank line, and the first line inside must look like a YAML key or directive. Without it a chapter opening with a slide split loses its first slide, and a chapter opening `---` above a heading would be read as metadata Pandoc itself would then fail on.
- 2026-09-05 — A `.variant` div or span whose `variant` is empty is marked but unresolved, not "every variant": it belongs to a reserved name no author can write, so text the author has not finished addressing is rendered into no variant rather than into all of them.
- 2026-09-05 — Gated stays selectable and is recorded, rather than being disabled and refused as the publish spec says. The flag is the history of the choice Alice made at each publish; phase 1 enforces neither gate, and the panel says so in a line beside the choice — `GATED_NOTE`, "recorded in the publish log and not enforced in phase 1". Recorded as a departure from the spec's "Gated present and disabled", because a control that records the truth is worth more than one that is greyed out.
- 2026-09-05 — The publish panel takes `C-c C-l`, not the `C-c C-p` its spec names: `C-c C-p` is Present's row in the binding table, and one chord cannot be two commands. The spec's Panel section is the departure.
- 2026-09-05 — The dry run is a second command, `publish_dry_run`, rather than a `dry_run` field on `PublishRequest` as the spec's command table has it. One request shape that can ask for either makes the network-touching path and the network-free path the same call site; two commands make the capability boundary the command name.
- 2026-09-05 — A published version carries only the picture formats the app reads — `ASSET_EXTENSIONS` — held at both ends: `src/publish/build.ts` refuses any other reference and reports it, and `stage_version` refuses any copy whose source or target carries another extension. A local video source is therefore a build refusal in phase 1 rather than a file on the site.
- 2026-09-05 — No rendering writes a `style` attribute. The site serves under `style-src 'self'`, which drops one unread, so a declared column share or picture width reaches the page as `data-width` — a whole percentage on a scale of fives — and a pipe table's alignment as `data-align`, with the rules in `slides.css` and `presenter/article.css`. A deck that laid itself out inline would have collapsed at the link while looking right in the app.
- 2026-09-05 — A publish pushes whenever `HEAD` is ahead of the remote-tracking branch, not only when this publish committed. A publish whose push failed leaves a commit behind, and the retry writes nothing new — so the old rule skipped the push and then recorded links the site had never received.
- 2026-09-05 — `check_deploy`, the one outbound call in the crate, is gated on `PublishInProgress`: the publish command opens a ten-minute window on the hash it pushed, and a poll for any other hash, or outside that window, is refused before a request is built.
- 2026-09-05 — The publish remote must be a name `git remote` lists, never a URL, and `GIT_PROTOCOL_FROM_USER=0` is set on every git invocation: the settings come from the web view, and a remote naming `ext::` or another local transport would run a command of its own.
- 2026-09-05 — The site's base URL must be `https`. An unlisted link is protected by being unguessable, and `http` puts it on the wire in clear.
- 2026-09-05 — A second drop of the same bytes into another Part copies them again rather than writing a reference that climbs out of the Part. A Part is self-contained: `../01-part/assets/x.jpg` is a reference the publish build refuses and one that breaks the moment either Part is moved. The manifest therefore carries one entry per copy, matched on the whole digest and the path rather than on the sixteen-character id.
- 2026-09-05 — GIF comment and application blocks and SVG comments, metadata elements and local-path attributes are stripped like EXIF; an AVIF that declares EXIF or XMP is refused instead, because taking a box out of an ISO base media container means rewriting every offset through it. A JPEG whose marker walk goes wrong is treated as carrying metadata rather than as carrying none.
- 2026-09-05 — A phone-native decode is capped at 64 MiB of source and 80 megapixels decoded. A decode is the one place a dropped file's own bytes decide how much memory Editor asks for, so the ceiling is stated rather than discovered.
- 2026-09-05 — The article's contents list covers every chapter, and each chapter's ids carry a prefix of its own (`c1-`, `c2-`, …). An outline id is unique within one chapter only, and the article is one page per document, so without the prefix two chapters opening the same way would send both contents entries and both footnote anchors to the first one.
- 2026-09-05 — A rendering writes an `href` only for `http`, `https`, `mailto` and a relative reference. A chapter is a file and a file can come from anywhere, so an address in one is the author's text: a `javascript:` or `data:` video source is listed as its own words and handed to no reader's browser.
- 2026-09-05 — Present builds the document's default variant, carried on `DeckSource` from the shell, and the renderers filter `.variant` spans as well as `.variant` blocks. Otherwise the deck rehearsed and the deck published were two different decks, and a picture inside a variant span shipped in every variant's assets.
- 2026-09-05 — A publish refuses a dirty buffer, a chapter that would not read, and a document that declares no variant, before anything is built. A version at a public link cannot be taken back, and the rule lives in `documentForPublish` where it is testable rather than in the shell's entry point where it was not.
- 2026-09-05 — `capabilities/present.json` says plainly that Tauri's capabilities do not gate the application's own commands: every command in `generate_handler!` is reachable from either window, and what keeps the present window to `pending_deck` and `read_asset` is the page it loads. The earlier description claimed an isolation the file does not provide.
- 2026-09-05 — The fenced-div canon is read by a hand-written markdown-it block rule in `src/core/markdown.ts` rather than by `markdown-it-container`, which is therefore not a dependency. The canon writes nested divs with three colons at every level, as Pandoc does; `markdown-it-container` closes the outer div at the first bare `:::`, which turns the canon's own columns example into three sibling divs and swallows the paragraph after it. The rule counts opening and closing fences instead.
- 2026-09-05 — The publish log records the first sighting of a version at its link as `seen_at_link` on the entry, replacing the earlier `deploy` field: it is a stamp on the entry the publish wrote rather than a second record of the same publish, and `record_seen_at_link` writes it once.
- 2026-09-05 — The core's own `deckDocument` envelope and the no-op `siteResolver` alias are removed. The envelope that ships is the site build's, because the site's `script-src 'self'` forbids the inline script the core's envelope started the engine with, and a second envelope nothing writes is a second answer to one question.
- 2026-09-05 — `docs/reference-the-markdown-canon.md` is generated for real: `tools/generate-canon-reference.mjs` reads `src/core/canon.ts` and `src/core/inserts.ts` through Vite's SSR loader — the same modules the program imports, so the page cannot disagree with it — and writes the forms and the placement table between markers, leaving the hand-written prose alone. `npm run lint` fails when the page is stale, and `npm run docs:canon` rewrites it.
- 2026-09-05 — Option is Meta, so an Option keydown never produces text. `src/editor.ts` claims, at the lowest precedence, any Option keydown whose `key` is one character or `Dead`, and cancels the `compositionstart` and `beforeinput` events that follow; keys that produce no text (`M-Up`, `M-Backspace`) are left to the keymaps that bind them. macOS composes `ƒ`, `∂` and five dead-key accents on Option, and an editor that reads Option as Meta cannot also let it type. The key log therefore reports *claimed* and *handled* separately: an unbound Option chord is claimed and not handled.
- 2026-09-05 — The key spike's manual checklist gains an automated half behind two environment variables, `EDITOR_OPEN_FOLDER` and `EDITOR_KEY_LOG`, both unset on every ordinary launch. The log path is resolved in the shell from the launching environment and confined to the application's cache directory or the system's temporary directory, because the web view is a trust boundary and the page must not be able to name the file it writes to.
- 2026-09-05 — The app exports the deck and the article as folders the author chooses, through an export panel on C-c C-e, using the one builder the publish path uses. Reverses the morning's "single HTML file is the only local artefact" after the maintainer used the app; the single file stays intent 17.
- 2026-09-05 — The document metadata gains `fill_column` (default 80) for fill-paragraph, and the byte-fidelity rule states that a command the author invokes on purpose edits exactly the spans it names.
- 2026-09-05 — An exported deck's page sits at `slides/index.html` inside the folder, not at its root, and the folder carries its own copy of the chrome under `presenter/`. Keeping the exported folder shaped like a published version folder is what makes every asset reference identical to the staged one, so the two builds differ only in the chrome base and can be compared byte for byte; the cost is that the file Alice double-clicks is one folder in, and the panel's row says so.
- 2026-09-05 — Focus is a model of its own (`src/focus.ts`): three panes — editor, sidebar, whatever panel is open — in the one fixed order `C-x o` and `C-x C-o` walk. The binding table gains a second scope rather than a second table: `scopeOf` says which pane answers a row, `chordIndexIn` states the no-two-rows-one-chord invariant per scope, and so `C-n` is next line in the text and next node in the tree without either being ambiguous.
- 2026-09-05 — A pane the editing surface cannot hear is read by one document keydown listener in `src/focus.ts`, active only while the keyboard is elsewhere. It is a second reader, not a second prefix state: the half-typed chord it holds is fed to the modeline's own prefix cell, so a `C-x` typed in the sidebar looks exactly like one typed in the text.
- 2026-09-05 — The `other-window` row moves from owner `app` to owner `editor`, because it is now answered rather than reserved. Both its chords stay: `C-x C-o` sits beside `C-x o` so the Control key need not be lifted between the steps.
- 2026-09-05 — A panel joins the pane cycle by handing `registerPanel` a focus contract (label, element, `isOpen`, `focus`) and by nothing else; it keeps its own cancel handling. The model learns a panel has taken or lost the keyboard from a document `focusin` listener and from `onOverlayChange`, rather than from a `close` of its own, so the one cancel contract stays the panel's and the modeline still never names a pane that does not have the keys.
- 2026-09-05 — `C-x C-c` asks in the window, not through the system dialog: a two-row overlay ("Quit without saving", "Keep editing") in the application's overlay host, so `C-g` and Escape return Alice to the text with her edits intact. The intent asks that `C-g` answer the quit question and that Editor ask "exactly as closing a chapter does", and both cannot hold today — the window-close path asks through `services.confirmDiscard`, a native dialog that answers Return and Escape alone. The close path is left as it is; adopting the overlay there is a later intent's decision.
- 2026-09-05 — The sentence rule ships as a `ProseOptions` field, `sentenceEndDoubleSpace`, defaulting to Emacs's own rule: a sentence ends at `.`, `?` or `!`, then any closer, then two spaces or a line break. It is in code and in tests but in no panel, because `02-constraints.md` fixes the `document.yaml` key list and it carries no such key, and `settings.json` is machine facts only. Where its off switch lives stays open.
- 2026-09-05 — `M-x` and `M-/` come off the suppression list, because both now have something behind them: the command palette is a view over the binding table and the insert-form table, and `M-/` completes from the document's own words. The list overlay the insert palette used is extracted into `openListOverlay` and the command palette and the quit confirmation open the same one, so the three cannot drift; `overlay.ts` gains one optional `onKey` hook, which is how the `M-z` and `C-h k` prompts hold the keyboard under the one cancel contract rather than a second copy of it.
- 2026-09-05 — `C-h k` answers against `chordIndexIn("editor")` rather than `chordIndex()`. The chord is pressed in the text, so the editing surface's scope is the honest answer; the flat index would report two rows for `C-n`, which is next line in the text and next node in the tree and never both.
- 2026-09-05 — An overlay carrying an `onKey` hook listens for keys on the window rather than on the document, one node above the pane cycle's own reader. A list overlay stays on the document, so `C-x o` can still take the keyboard out of it; a prompt is not a pane to cycle out of, and without the higher target a `C-x` typed into `C-h k` was read as the first step of `C-x o` and the prompt only ever saw the second step.
- 2026-09-05 — This project does not honour `/*@__PURE__*/` annotations. `vite.config.ts` sets `treeshake: { annotations: false }` at both halves of the toolchain — `optimizeDeps.rolldownOptions` for the dev server's pre-bundle and `build.rollupOptions` for the release build — because `@replit/codemirror-emacs` annotates the two module-level calls that are its entire installation, and dropping them left the running app with a keymap that had no bindings and no commands while every test passed. The setting can only ever keep code the annotation would have removed. `tools/check-bundle.mjs` reads the emitted bundle after `vite build` and fails the build if the loop or the command table is gone; the running application says the same thing in the modeline (`no keymap`), because a console error is seen by no one who is not already looking.
- 2026-09-05 — The present window logs its own state to a file named by `EDITOR_PRESENT_LOG`, through one command, `present_log`, unset on every ordinary launch. The path is read from the environment the process was started with, never from the page, and confined to the cache or temporary directory the way `EDITOR_KEY_LOG` already is; a name a symlink is standing at is refused rather than written through, a line is capped and may carry no line break, and the page latches the log off the first time the shell answers that there is none, so a dragged resize does not cross to the shell once a frame.
- 2026-09-05 — `other-window` ships in `group: "document"`, not the `control` group its spec's table names. The groups are what the keys panel lists under, and the row belongs beside the document-wide chords Alice reaches from the text rather than in the control group, which holds the chords that act on the running application. Recorded as a departure from spc-2609051925374395's binding table, which is the authority on everything else about the row.
- 2026-09-05 — An export's destination is not the page's to name. The folder dialog is opened by the shell (`choose_export_destination`), the answer stays there under a nonce that expires in two minutes and is good for one export, and `export_rendering` writes into the folder that nonce claims and no other — the same shape `assets::DropQueue` already uses for a drop's paths. The export also refuses the application's own cache and both staging trees, creates its folder with `create_dir` rather than `create_dir_all`, and reveals the folder it actually wrote to.
- 2026-09-05 — The export intent's byte-identity criterion is narrowed: assets and slide markup identical to the staged version, the deck page differing only in the chrome attributes that point beside it, because the site keeps its engine at the root and a folder must carry it alongside. Orchestrator's call under the standing authorisation; the maintainer may strike it instead.
- 2026-09-05 — An overlay that loses the keyboard is closed, not left open. `openOverlay` holds the keyboard with a document-level capture listener, and `C-x o` moves the keyboard without that listener knowing: the quit question left behind the buffer went on claiming `Return`, so an ordinary newline answered it and threw the unsaved edits away (iss-2609052115254279). `focus.ts`'s `panelTarget.release` now closes the overlay in the third place, cancelled — which for the quit question means keep editing — while a *registered* `PanelFocus` (publish, settings) keeps the stays-open behaviour, because it has no reader of its own to leave running.
- 2026-09-05 — `M-x` offers `editor`-scope rows only, and `runBinding` refuses any other scope before it looks at a chord. A chord is unique only inside a scope, and `runBinding` resolves a row it does not own itself by chord: `Return` is "Open the chapter here" in the tree and a newline in the text, so the six sidebar rows the palette listed inserted newlines instead of opening chapters. Both ends refuse, so neither the list nor the runner is the single point of trust.
- 2026-09-05 — The quit confirmation opens on "Keep editing". An overlay opens on its first row and `Return` takes it, so the destructive answer is never the one a reflex reaches; she moves onto "Quit without saving" before Return means it.
- 2026-09-05 — The pane a keyboard is in is the editing *surface*, not the content: `FocusHooks.editorSurface` is `view.dom`, so CodeMirror's own search panel counts as the editor. Focus lost from the tree is taken back only when nothing is holding it — the body, or nothing at all — because taking it back moves the caret, and a focusable element that is not a pane is holding the keyboard for a reason.
- 2026-09-05 — A failed export takes its own folder away again. Everything written after `create_dir` has one exit (`staged_into`), and any failure on that path removes the folder: an empty or half-filled folder standing in the destination looks like an export that worked and forces the next attempt to name itself `-2`. `write_chrome`'s messages name the file by its path inside the folder, never on the machine, because they cross the IPC into the panel. `DESTINATION_LIFETIME` is thirty seconds — the round trip it has to cover — and the nonce's expiry is tested on an injected clock rather than by waiting.
- 2026-09-05 — `append_log_line` asks its symlink question twice: once of the name, and once of the open handle, comparing device and inode. A name can be swapped between the check and the open; a file descriptor cannot, so the residual the check-then-open note named is closed without adding a dependency for `O_NOFOLLOW`.
- 2026-09-05 — `tools/check-bundle.mjs` looks for `killLine` as a property of the object `addCommands` is called with, because the application carries the same name as a string constant and a plain `includes` was answered by it. The guard follows the entry chunk's own imports and passes when the keymap is whole in any chunk the entry loads, failing only when no chunk on that graph carries it: a chunk split is not a keymap dropped, and a whole chunk the entry does not load is a wiring failure reported as one.
- 2026-09-05 — Filling keeps a hard break. Two or more trailing spaces and a trailing backslash are breaks the author wrote, not whitespace to collapse; the paragraph is filled in the pieces they divide it into and each keeps the bytes that ended it. `M-/` folds case when it searches and puts back the case that was typed, which is Emacs's own rule and the one an author starting a sentence needs.
- 2026-09-05 — Prose commands read the document through `state.sliceDoc`, not `doc.toString()`/`sliceString`, wherever what they read is written back. The latter join with `\n` whatever the chapter's separator is, so on a `\r\n` chapter `M-q` compared unequal against itself for ever and `M-t` wrote a bare `\n` into the middle of a line.
- 2026-09-05 — `M-z` reads a chord, not a keystroke. `event.key` is `a` for `C-a` as much as for `a`, so a modified chord typed at the prompt zapped to the letter under it; Control, Option and Command are refused as a target and Shift is not, because Shift is how a capital is typed.
- 2026-09-06 — The editing surface's type scale is five steps of 1.2 either way, bounded at about 0.40 and 2.49 of the default, and it is a property of the CodeMirror view alone: a theme on `.cm-editor` held in a compartment at `Prec.highest`, because the surface's base theme sets `fontSize` on the same selector and without the precedence the two rules tie and the base one wins. The step is clamped in the page and again in `read_settings`, so a `settings.json` edited by hand to 40 opens a surface the author can still read. `C-x C-0` needed one piece of surgery: `EmacsHandler.findCommand` reads Control-and-a-digit as the start of a numeric argument before it consults its own key chain, so the chord set a count and left the chain half-open; the guard in `src/emacs.ts` answers it only when the chain is already open and the chord is one Editor registered itself, beside the `REBOUND` list that already covers the package's other unreachable spellings.
- 2026-09-06 — The present window imports the deck engine as an ES module (`src/vendor/reveal/reveal.esm.js`) instead of reading the `Reveal` the UMD build assigns to `globalThis`. The UMD file means one thing to a bundler, another to a dev server and a third to jsdom, and the bundler's reading — CommonJS, `module.exports` — assigns no global at all, so the shipped window mounted a deck with no engine while every check stayed green (`iss-2609061132369973`). Both builds stay vendored: the app's window goes through a bundler and the published site and the single-file export serve the UMD raw under `script-src 'self'`, so neither can use the other's. `reveal.esm.js` is the one vendored file the toolchain reads through, so its `sourceMappingURL` comment is cut on every refresh and its half-megabyte map is not vendored: a comment pointing at a map that is not here is a warning on every build. `tools/check-bundle.mjs` asks the emitted present chunk three questions — the engine is in there, the accessor resolves to that engine, and the mount takes that accessor as a parameter default — because every source-level check passed on the build that shipped.
- 2026-09-06 — The launch harness (`EDITOR_OPEN_FOLDER`, `EDITOR_KEY_LOG`, `EDITOR_PRESENT_ON_OPEN`, and `EDITOR_PRESENT_LOG` beside them) stays live in release builds rather than compiling out under `debug_assertions`. It is what produces the logs a release-only failure is diagnosed from — the last three bugs came out of runs against the release binary — and a switch that exists only in a debug build cannot answer a question about the shipped one. The surface it widens is confined: every switch is read once at start from the process environment and never from the page, the key log is refused unless it lands in the application's cache directory or the system's temporary directory, the page is told whether a log is being written and never where, and each switch is off unless a run asks for it in words (`1`, `true`, `yes`, `on`; `0`, `false`, `no`, `off`; anything else refused with a warning). `iss-2609061211328807` holds the choice open for the maintainer; this is the current one.
- 2026-09-06 — Phase two begins: the five article intents (map #9, #10, #11, #12, #26) and the phase-one leftover #29 are planned, one spec each because the binary plans no bundles (iss-2609051428335393); the outline vocabulary observation (iss-2609051934113806) is promoted to an intent; and the four ledger issues whose intents shipped in 0.2.0 and 0.2.1 are resolved against those intents with `--shipped-in` so the next cut does not report them twice.
- 2026-09-06 — Added the tenth discipline, Reachable by assistive technology (itd-2609061324342715), binding from phase 2 because the article is the first reading surface with a public audience: every surface must be keyboard-operable without the Emacs chords, expose native or ARIA semantics to a screen reader, and never carry state by colour alone; recorded in the Disciplines table of 07-intent-map.md and as a locked decision in 02-constraints.md, answering iss-2609051428298697.
- 2026-09-06 — Moved the reader-facing layer toggle from intent 24 (itd-2609051336040242) to intent 23 (itd-2609051336037640): a reviewer's three-reviewer-intent-review observation (iss-2609051428303830) found 24 folding Alice's review and publication of a layer together with Carol's toggle for it, when the toggle is what a reader sees and belongs beside her other reader-facing marks; 23 now carries the toggle's press-release paragraph, scope boundary, and three acceptance criteria (the control, its persistence, and its legibility at 390 CSS px), while 24 keeps review, the layer object and its name, and the guarantee that Alice's private marks never publish unmarked; the boundary sentence in 07-intent-map.md for both intent 23 and 24 is updated to match.
- 2026-09-06 — Amended ac-5 of the shipped sidebar-navigation intent (itd-2609051921482691) and its closed spec's Design and Acceptance Mapping (spc-2609051925374395) to state the cycle as built rather than as first promised: a registered panel (publish, settings) cycled away from stays open and holds the three-step walk editor-sidebar-panel-editor, while a modal overlay (the keys panel, the insert palette) closes the moment the keyboard leaves it (panelTarget.release, iss-2609052115254279), so its walk is the two-step editor-sidebar-editor with a negative case for the overlay staying open; this closes the NOT_MET recorded against ac-5 in the intent's Audit Notes without touching src/focus.ts, which already implements the rule the criterion now states, answering iss-2609052143457583. No docs page states the old three-step claim, so none needed amending.
- 2026-09-06 — Answered the maintainer's slide-mapping question (iss-2609061210010975) with the rule both readings agree on: the top heading level present in what is being presented runs horizontally and the next level hangs vertically, so one chapter presents its Sections across and Sub-sections down (unchanged, H1 the title slide) while a whole document of more than one chapter presents its chapters (H1) across, their Sections down, and Sub-sections folded into notes; a document of exactly one chapter is not shifted and presents exactly as buildDeck presents that chapter alone. buildDocumentDeck and buildChapterDecks in src/core/deck.ts did not follow this rule before the fix — every chapter's Sections were flattened onto the same horizontal line as every other chapter's, with no vertical grouping by chapter — so both now take a verticalFrom threshold (3 for one chapter, 2 for more than one) threaded through columnsOf, headingKind and openHeading, with buildDeck and the present window's single-chapter mapping unchanged (signatures of buildDocumentDeck and buildChapterDecks are unchanged, so publish/build.ts and export/services.ts needed no edit). Tests pin both shapes and the one-chapter-document-equals-buildDeck case in src/core/deck.test.ts; docs/explanation-the-document-model.md states the rule for both the projected chapter and the published document. Intent map #5 (itd-2609051335447894) scopes only the single-chapter mapping and states nothing the rule contradicts, so it is unchanged.
- 2026-09-06 — Closed iss-2609061211328807: the choice recorded above (the launch harness stays live in release builds, confined) is verified against src-tauri/src/devharness.rs and src/devharness.ts rather than reopened. Every confinement claim holds in code: devharness::configure runs once from app setup with no #[tauri::command] path back into it, so the environment is read once and never from the page; confine_log_path refuses a path outside the temporary directory and the application's cache directory (already tested for the temporary directory; a new test, takes_a_path_inside_either_confined_root, proves the OR by giving it two roots that are neither the real temp directory and showing the second is accepted on its own); HarnessSettings carries key_log as a bool with no path field, so the page cannot be told where; and the present_on_open switch is refused rather than guessed at for a word outside 1/true/yes/on and 0/false/no/off/empty (already tested). The one gap found was src/devharness.ts itself, which had no test file at all — added src/devharness.test.ts, covering the frontend half of the first two claims: dev_harness is invoked exactly once per run regardless of what the settings then cause, and every dev_log_key line this module writes carries no path. docs/spike-emacs-keys.md already states all four claims accurately and needed no edit; no other docs page names the harness.
- 2026-09-06 — Answered itd-2609051402191319's (map #29, spc-2609061318158586) three open questions: the default asset threshold is 8388608 bytes (8 MiB), the figure named in 05-internals.md section 6's own assets.json example and already carried by document.yaml in both of the maintainer's own documents kept for local testing (iss-2609061418065651); the default citation style is numeric, the value one of those documents' document.yaml carries and the one map #11's bibliography builder is written to honour; and the first Part a new document gets is named neutrally (chapters, giving 01-chapters) rather than from the title, because renaming a Part is renaming a folder and the intent itself flags the first Part as the one an author is least likely to ever rename — a title-derived name would go stale the moment the title changed, since nothing keeps the folder name and the title (which lives in document.yaml and the chapter's own heading) in step.
- 2026-09-06 — For itd-2609051402191319 (map #29), src-tauri/src/new_document.rs's slug rule deliberately departs from src/core/outline.ts's slugify: both are lowercase ASCII with non-alphanumerics collapsed to hyphens, but the frontend additionally folds accented Latin letters to their plain form through Unicode NFKD normalisation, which Rust's standard library has no table for and AGENTS.md forbids reaching for a dependency to add; the Rust rule instead treats any non-ASCII letter as ordinary punctuation and hyphenates it. The two rules agree on every title this repository's examples use and diverge only on accented or non-Latin script, and nothing reads a Part or Chapter name back through the frontend's slugify, so the two never have to agree with each other. Recorded rather than left as an unexplained inconsistency between the two slug implementations.
- 2026-09-06 — Settled the four chord conflicts `itd-2609061318091323` (map #34, `spc-2609061341298538`) inherited from the research note (`.abcd/development/research/notes/2026-09-05-emacs-vocabulary-gap.md`): for `C-c C-p` (present), `C-c C-l` (publish-open) and `C-x k` (toggle-key-log) the incumbent, shipped and documented meaning keeps its chord unchanged — none of the three rows, nor `main.ts`, `publish-panel.ts`'s `PUBLISH_OPEN_CHORD`, or the docs pages that name them, was touched — and the outline vocabulary's own previous-heading, insert-link and close-chapter take `C-c p`, `C-c l` and `C-x C-k` instead, following the `C-c i` precedent `insert-palette` already sets for Editor's own family beside Markdown mode's spelling. The fourth, `M-s` (`center-selection`), is retired outright rather than relocated: it names no doc, test or other module, and `C-l`'s own `recenter` already covers what it did, so `M-s` becomes the prefix `outline-occur` answers on `M-s o`.
- 2026-09-06 — For `itd-2609061318091323`, retiring `center-selection`'s `M-s` required adding it to `src/keys.ts`'s `SUPPRESSED` and reordering `src/emacs.ts`'s `registerEditorChords`: the suppression loop now runs before the application's own chords are bound, not after, because `EmacsHandler.bindKey` keeps only the last call for an exact key and `outline-occur`'s `M-s o` is a longer chain starting at `M-s` — binding it after the suppression clears `M-s` is what turns the chord into a working prefix rather than a dead key; the reverse order silently undid the prefix the moment the suppression loop ran, which the existing conformance sweep (`answers no chord the table does not list`) caught before the fix. The `why` field departs slightly from every other `SUPPRESSED` entry's sense (a chord Editor gives back to the browser): `M-s` does not fall through, it is claimed by Editor's own prefix, and both `src/keys.ts`'s row comment and `docs/how-to-find-and-change-the-keys.md` say so rather than leaving the table's usual reading to imply otherwise.
- 2026-09-06 — Departed from `spc-2609061341298538`'s first draft for `outline-cycle` (`S-Tab`, `itd-2609061318091323`): the draft answered it with `@codemirror/language`'s `foldAll`/`unfoldAll` directly, but `foldAll` walks from the document's start and a chapter's level-one title has no shallower heading to stop its own fold range at, so the first `S-Tab` would have collapsed the whole chapter body under its title rather than folding each top-level section — heavier than the acceptance criterion's "every outermost heading" asks for. `cycleOutline` instead dispatches one `foldEffect` per level-two heading (`core/outline.ts`'s own `FIRST_LEVEL`, the outline's own top), still stored and cleared by the same `foldState`/`unfoldAll` CodeMirror keeps; a chapter with no level-two heading at all finds nothing to fold. The spec's Design and Risks sections were amended to match what shipped.
- 2026-09-06 — For `itd-2609061318091323` (map #34), narrowing (`C-x n n`/`C-x n w`) is built as a visual aid over CodeMirror decorations rather than Emacs's true `narrow-to-region`: it hides every line outside the current section from the window by adding `display: none` to it through a dedicated `StateField`, but the document, the cursor, search and every other command still reach the whole chapter. This is deliberate (recorded as a scope condition on the intent, `cond-2609061344223793`) rather than a shortfall: because nothing about the model itself narrows, `documentText` — what every save reads — is provably identical whichever state the field is in, which is what lets the round-trip byte-fidelity discipline hold by construction; a stronger restriction is left to a later intent if the visual version proves not enough. `src/outline-commands.test.ts` proves the guarantee directly over the hazardous fixture `spc-2609061145242761`'s round-trip test uses.
- 2026-09-06 — For `itd-2609061318091323`, the research note's tier-two "chapter list" item is answered by the sidebar toggle map #1 already binds to `C-x C-b` rather than by a new row: opening or closing the sidebar already shows the chapter list the note names, and a second row bound to the same purpose would be two answers to one question. Only "switch chapter by name with completion" (`C-x b`) is a new row, wired through `openSwitchChapter` in `src/outline-commands.ts` over `app.chapters`/`app.openChapter`, the same list and the same open path the sidebar already uses (`itd-2609051335399446`).
- 2026-09-06 — map #9 (spc-2609061318090042, itd-2609051335489928): a margin note (footnote, citation, .margin span, .credit div) is written once as the block's own next sibling and rendered through one generic renderMarginNote, so the fold at 390 CSS px and the sit-beside at 820/1280 are one stylesheet breakpoint over one markup shape rather than two code paths; the citation's body is the unresolved key marked "citation unresolved", the seam itd-2609051336090390-owning map #11 replaces later without touching placement.
- 2026-09-06 — map #9 (spc-2609061318090042): the app's article preview is a second window (preview.html/src/preview.ts/src-tauri/src/preview.rs), in the manner of present.html/present.ts/present.rs, opened by C-c C-v; unlike Present, which hands the present window one chapter's buffer text, Preview hands the preview window every chapter's — the open one from the buffer so an unsaved edit previews, the rest read fresh from disk — because the article is one page for the whole document rather than one chapter's deck.
- 2026-09-06 — map #9 (spc-2609061318090042): the document-wide contents list groups chapters by Part, using a new partTitleOf in src/publish/build.ts mirroring title_of in src-tauri/src/document.rs, and renders a Part's own entry as a <span class="part"> label rather than a link, because linking it to its first Chapter would duplicate that Chapter's own href in the same nav — caught by src/publish/build.test.ts's pre-existing href-uniqueness assertion rather than assumed up front.
- 2026-09-06 — map #9 (spc-2609061318090042): src/core/render/article.css and src/core/render/article-video.js become the one core source for the article's stylesheet and script, on the model slides.css already set; site/presenter/article.css (a hand-kept copy) is removed, and src-tauri/src/publish/stage.rs's CHROME/chrome_for_folder now include_str! the core files directly, so a fix to either reaches the app's preview, the site build and the folder export at once.
- 2026-09-06 — Untracked examples/ (iss-2609061418065651): git rm -r --cached with the files kept on disk, gitignored outside the managed abcd block, and every code/docs/record reference to a named example folder rewritten to a generic description or removed; the two real-document snapshot-dependent test files (core/examples.test.ts, core/tree-examples.test.ts) and their snapshots were deleted rather than kept passing against a folder that may not exist, their assertions either folded into the new src/local-documents.test.ts harness or already proven generically by deck.test.ts, slides.test.ts and assets.test.ts with synthetic fixtures. Added a second local document — a real single-file manuscript the maintainer supplied, converted with a gitignored one-off script under .abcd/.work.local/scratch/ into the same folder-of-chapters shape — so the harness proves itself against more than one real document; src/export/services.test.ts and src/publish/build.test.ts still read a document folder under that untracked root directly and were left alone (owned by the concurrent citations work) and captured as iss-2609061441525077 rather than fixed.
- 2026-09-06 — Fixed iss-2609061437127301: src-tauri/src/metadata.rs's synthetic PAPER_METADATA fixture used Rust's \n\ line-continuation escapes across an indented YAML block scalar; a backslash-newline in a Rust string literal strips the following line's leading whitespace along with the newline, which flattened the block scalar's indentation and broke parsing. Rewritten with real embedded newlines in the string literal instead.
- 2026-09-06 — map #11 (spc-2609061318151591, itd-2609051335502171): built src/core/bibliography.ts as the one BibTeX reader and resolver every rendering calls through (05-internals.md section 4), rather than a per-renderer implementation; resolveCitations is called once per document, in preview.ts and publish/build.ts, and the same CitationResolution is handed to every chapter's renderArticle and to buildChapterDecks, which is what the renderings-agree discipline (itd-2609051336130664) rests on.
- 2026-09-06 — map #11 (spc-2609061318151591): departed from the build prompt's fallback instruction to implement citation completion and hover with src/overlay.ts's list-overlay machinery; that contract holds the keyboard while it is open, right for a panel Alice opens and closes but wrong for an in-buffer completion that must keep receiving her keystrokes while it narrows, so src/citations.ts is built on @codemirror/view's own showTooltip and hoverTooltip instead — both already a direct dependency, so no dependency was added and @codemirror/autocomplete (only a transitive one) was not reached for.
- 2026-09-06 — map #11 (spc-2609061318151591): the citation-completion keymap's Tab-to-accept binding is placed in src/editor.ts's Prec.highest array ahead of the Emacs keymap, the same position markdownReturn already holds, because Tab is already the binding table's own chord for outline-toggle-fold and the Emacs plugin claims it unconditionally; the citation binding declines (returns false) whenever no completion is open, so an ordinary Tab is unaffected.
- 2026-09-06 — map #11 (spc-2609061318151591): a document that writes no .refs heading gets its generated reference list appended once, after the last chapter, with no invented heading text, because neither the intent nor the brief names a default heading for that case; an author who wants one writes ## Sources {.refs} and renderOneBlock's own appendix placement puts the list there instead, through chapterHasRefsHeading.
- 2026-09-06 — map #11 (spc-2609061318151591): a Section's own citations earn the deck a credit line per slide, deduplicated within that slide only (not across the whole deck), mirroring how deck.ts's existing footLinesFor already treats a footnote; a work cited on two different slides is credited on both, which is deliberate per-slide sourcing rather than a document-wide one-time list, matching the intent's 'a deck has a credit line per slide and nothing else'.
- 2026-09-06 — map #11 (spc-2609051335502171 build): resolved iss-2609061441525077 by rewriting src/publish/build.test.ts's talk fixture and src/export/services.test.ts's document() fixture to build a synthetic two-chapter DocumentSource from inline Markdown strings rather than readFileSync-ing a folder under the now-untracked examples/ root, since both files are this intent's own to touch (renderVariant's bibliography wiring) and the coordinator asked that they be fixed rather than left captured; the invented fixture had itself been named after a real folder still kept under examples/, so it was renamed again to an unambiguously invented title ("the-lantern-papers") in src/export/services.test.ts, the file this pass owns.
- 2026-09-06 — map #10 (spc-2609061318154502, itd-2609051335492327): the reader-controls toolbar (article-controls.js) sets spc-2609061318090042's own six custom properties and one new data-article-theme attribute on the root element, doing all its work through localStorage under one key wrapped in try/catch, and never calling fetch or XMLHttpRequest — proven by a test that spies on both across every control and the reset; the toolbar is native buttons inside fieldset/legend groups with aria-pressed, and never role="toolbar", because that role commits to an arrow-key roving-focus pattern this control does not implement (itd-2609061324342715).
- 2026-09-06 — map #10 (spc-2609061318154502): the toolbar's measure control uses rem for its narrow/wide steps rather than the em spc-2609061318090042's own default measure uses, because rem is relative to the root element's font size (untouched by this map's own text-size control) while em is relative to the local, text-scale-affected one; this keeps the measure and text-size controls independent so choosing the largest text and the widest measure together never compounds into a desired width neither control alone intends, and block-level max-width already caps the rendered width to the viewport regardless of unit, so no explicit min() against 100vw is needed.
- 2026-09-06 — map #10 (spc-2609061318154502): the fold breakpoint and the margin rail's own reserved-width arithmetic in article.css are left unchanged by this map, on the finding that the reserved margin strip's width depends only on --article-gutter and --article-margin-width, never on --article-measure — so widening or narrowing the measure changes how much of the text column's own width is on offer without ever moving the margin column, which is what keeps a margin note level with its paragraph as the measure changes (itd-2609051335492327's own falsifiable claim); recorded because the intent's own Mechanism section raised the question, and the answer was found by reading the existing rules rather than by writing a new one.
- 2026-09-06 — map #10 (spc-2609061318154502): src-tauri/src/publish/stage.rs's CHROME array and chrome_for_folder("article") both gain a third entry, presenter/article-controls.js, the same one-source arrangement article.css and article-video.js already have; src-tauri/src/export.rs's two hard-coded article-chrome-file tests are updated to the three files (alphabetical order puts article-controls.js before article-video.js, whose hyphen sorts before article.css's dot).
- 2026-09-06 — iss-2609061510051784: C-v (and M-v, PageDown/PageUp, C-Down/C-Up) is rebound in src/emacs.ts from the shipped package's cursorPageDown/cursorPageUp — which moves point by a full page's worth of vertical distance unconditionally and, for any chapter shorter than a screen, lands it on the document's last or first line — to a new scrollByPage that moves scrollDOM.scrollTop directly and leaves point untouched; the mark-active half of goOrSelect (selectPageDown/selectPageUp) is kept because nothing reported it broken. Diagnosed against @replit/codemirror-emacs's own findCommand and PACKAGE's emacsStyleKeymap/standardKeymap (Ctrl-v/cursorPageDown), not assumed. Separately, C-c C-v was unreachable because 'preview' was missing from APP_COMMAND_IDS in src/emacs.ts, so the C-c prefix (already opened by 'present') had no second-step binding for C-v and the keydown fell through unclaimed; adding 'preview' to APP_COMMAND_IDS fixes it. The swallowed-chord guard the text-scale spec (spc-2609061145242761) added was checked as a candidate mechanism and found not to apply here — that guard answers Control-and-digit chords findCommand reads as a numeric argument before consulting its key chain, a different defect class from a plain missing command registration — so it was left alone rather than generalised for a case it does not cover. A first cut moved point back onto the newly-scrolled page with an in-command view.dispatch, but CodeMirror's own scroll-anchoring reset scrollDOM.scrollTop right back on that same dispatch (measured in jsdom, not assumed to be jsdom-only), so the simpler and more robust fix leaves point alone entirely on a plain scroll.
- 2026-09-06 — iss-2609061514457567: src/core/render/slides.css scopes the fluid layout's `transform: none !important` on `.reveal .slides > section` to `.reveal:not(.overview)`, and adds a `.reveal.overview` block forcing `visibility: visible !important`/`pointer-events: auto` and, for non-stack top-level sections, `display: flex !important` on every slide rather than only `.present`. Diagnosed against the vendored src/vendor/reveal/reveal.esm.js: Overview.activate() adds the `overview` class and calls its own layout()/update(), which write each slide's own inline `element.style.transform` (a translate3d grid offset) and pan/scale `.slides` — both left inert by the unconditional !important, and by only `.present` ever losing `visibility: hidden`, which is exactly the stacked-on-the-first-slide-empty-elsewhere symptom the issue reported. A jsdom test in src/present.test.ts drives the real vendored engine (freshEngine(), the same fixture 'the deck the real engine is holding' already uses) through its own toggleOverview()/isOverview() API (Ct's public surface, not the .overview:de facade Object.assign(l,{...}) leaves off Ct — a wrong turn corrected mid-investigation) and asserts every top-level slide gets its own distinct, non-empty transform and visibility:visible while overview is active, and that both clear again on toggling off; it was run against the unmodified stylesheet first and failed on the visibility assertion exactly as diagnosed. src/present.ts also gains a transform field on BoxReport and two new recorded phases, overviewshown and overviewhidden, wired to the engine's own events, so a scripted run's log shows the slides spread out rather than only describing it. Proven on the npm run tauri build -- --bundles app bundle by launching with EDITOR_PRESENT_ON_OPEN/EDITOR_PRESENT_LOG/EDITOR_OPEN_FOLDER=a local document folder and reading back a correct mount/ready pair from the real engine; toggling overview itself through a live Escape keypress could not be exercised in this sandboxed session because osascript has no assistive-access (Accessibility) permission for GUI scripting and no other non-interactive route exists to send a real key event to a released app bundle, so that one step remains a manual check for the maintainer, while the jsdom test against the real engine is the automated proof the CSS answers the class and events the engine actually produces.
- 2026-09-06 — iss-2609061513087574: amended spc-2609061318090042's Risks note rather than changing preview.html or the build: a fresh npm run build and an inspection of dist/ confirmed vite build genuinely drops the separate <script src> tags for article-video.js, article-controls.js and article-eggs.js and merges all three into preview.ts's own output chunk, contradicting the note's original claim that a module-tagged script reference is preserved as its own copied file — but loading that exact built chunk into jsdom (dist/assets/preview-*.js, imported directly, not the source files) showed ArticlePage, ArticleVideo and ArticleControls all defined and the reader-controls toolbar's markup appended to document.body, because each of the three is a plain global-attaching IIFE with zero import/export statements and each falls back to its own unconditional DOMContentLoaded boot when ArticlePage.register is not there to call, so the merge changes nothing a reader or the video/controls behaviour depends on. Resolved as internal rather than fixed: the three files' own defensive boot design is what makes the merge harmless, not a defect to patch, and tools/check-bundle.mjs gains no new gate because there is no broken behaviour to guard.
- 2026-09-06 — map #12 (spc-2609061318159422, itd-2609051335518134): an .egg block's own `label` attribute, not the marker's own bracket text, is what the marker renders as once matched and what the tray entry reads back through the template's `data-egg-label`, decoupling display from the marker's original position in the paragraph; the marker's own written text is used only as the fallback an orphan marker shows (no block to draw a label from), which is also what a plain Markdown reader already shows a `[✦]{.egg …}` span as.
- 2026-09-06 — map #12 (spc-2609061318159422): article-eggs.js upgrades a video inside an opened egg panel with a small, locally-scoped function reusing ArticleVideo.chooseSource and ArticleVideo.probe directly, rather than calling ArticleVideo.upgradeVideos(); that function walks every figure.video on the page with no guard against one already carrying data-player-ready, so calling it again after a panel opens risked a second <video> beside an already-upgraded figure elsewhere in the article — found while designing the panel, not assumed, and left as article-video.js's own file untouched since no caller before this one ever asked it to upgrade the same figure twice.
- 2026-09-06 — map #12 (spc-2609061318159422): the once-only opening's flow markup carries no hidden attribute and no script-conditional branch in article.ts; article-eggs.js alone takes it out of the flow (moving the same DOM node into a modal, never cloning it) the first time a browser has not recorded editor-article-opening-seen, which is what makes the no-script degrade (itd-2609051336110536) a fact about the markup rather than a claim requiring a script to falsify.
- 2026-09-06 — map #12 (spc-2609061318159422): sidebar.ts's ChapterFacts.unresolvedEggs is optional rather than required, unlike unresolvedCitations, so an existing literal built as {unresolvedCitations: [...]} (map #11's own tests, and any other caller not yet updated) keeps compiling; badgeSlot reads it with the same ?? [] fallback either way, and app.ts's own readSidebarData always supplies it when it sets any ChapterFacts at all.
- 2026-09-06 — map #35 (itd-2609061509393380, spc-2609061617208646): C-x C-o moves from other-window (a second way to move to the other pane, beside C-x o) to a new row, open-file-or-folder, because Emacs's own delete-blank-lines already spends that chord and nothing in the binding table answers it; other-window keeps C-x o alone, exactly as Emacs does, and the pane-cycle spec (itd-2609051921482691) is otherwise untouched.
- 2026-09-06 — map #35 (spc-2609061617208646): C-x C-o opens a small list-overlay asking 'a document folder' or 'a single file' before either native dialog, rather than one combined picker, because tauri-plugin-dialog 2.7.3's FileDialogBuilder (backed by rfd 0.16) builds pick_file and pick_folder from the same NSOpenPanel with canChooseFiles/canChooseDirectories set the other way in each, with no public option to combine them — read from rfd's own macOS backend (panel_ffi.rs) rather than assumed — and adding a dependency to reach a combined mode is not this change's to spend.
- 2026-09-06 — map #35 (spc-2609061617208646): the pick-then-claim flow for C-x C-o lives in a new src-tauri/src/open_source.rs with its own nonce store (OpenSourcePicks), a third store beside export::ExportDestinations and new_document::NewDocumentDestinations rather than a share of either, because it answers a different question again: what either dialog produces is a pick spent by opening it, not a destination a later write claims. DocumentRoot::set widened from private to pub(crate) so open_source.rs can set the root the same way lib.rs's own open_folder command already does.
- 2026-09-06 — map #35 (spc-2609061617208646): a bare Markdown file opens through a new document::read_single_chapter that builds its one-chapter DocumentTree by hand rather than calling read_tree on the file's parent folder, because a whole-folder walk would also draw whatever else happens to share that folder — another manuscript, a stray note — and 'a one-chapter document' the intent asks for means exactly the one chapter picked, not every sibling. The root Part's own path and chapters carry the folder and the one file directly (parts: []), which is the same shape src/sidebar.ts already draws with no empty Part row for a real single-Part document.
- 2026-09-06 — map #35 (spc-2609061617208646): whether a picked file already belongs to a real document is answered by walking its folder's ancestors for one carrying document.yaml (open_source.rs's document_root_above), because a chapter can sit arbitrarily deep under nested Parts with no shallower bound than document::read_part's own MAX_DEPTH on the way down; the walk stops at the first document.yaml found or the filesystem's own root, and a chapter the resulting real tree cannot resolve (a symlink, a failure read_tree already records) opens the document with nothing pre-selected rather than refusing the whole open.
- 2026-09-06 — map #26 (spc-2609061318158216, itd-2609051402083398): the reading views' own vocabulary is nine existing rows of src/keys.ts's table — outline-next-heading/outline-previous-heading for Section movement, next-line/previous-line for item movement, outline-occur for the contents list, isearch-forward/isearch-backward for search, keyboard-quit for cancel, and keys-panel — recorded as READING_BINDING_IDS rather than an additive field on Binding, because a separate exported list, tested to name only ids that already exist in BINDINGS, touches src/keys.ts by one array rather than by widening every row's own shape.
- 2026-09-06 — map #26 (spc-2609061318158216): outline-occur (M-s o), not outline-switch-chapter (C-x b), is the contents chord, because switch-chapter names moving between different open files on the editing surface — a concept the article's one page has no equivalent of — while occur's own shape (a list, choosing one moves the position and closes it) already matches 'opens a list overlay ... Return jumps, cancel closes'; the overlay's own rows are read back from the page's already-rendered <nav class="contents">, through a new contentsEntries() in article-keys.js, rather than a second hierarchy walk of the document.
- 2026-09-06 — map #26 (spc-2609061318158216): src/core/render/article-keys.js reimplements src/keys.ts's chordFromEvent as an independent, line-for-line copy rather than importing it, because the article's script runs on the published site under script-src 'self' with no bundler and the intent's own Excluded condition (cond-2609061318155666) puts the mapping's redesign out of scope, not its faithful reuse; src/core/render/chord-mapping.fixture.ts is the one list article-keys.test.ts drives both chordFromEvent and the script's own copy against, asserting they agree rather than only that each matches the fixture in isolation.
- 2026-09-06 — map #26 (spc-2609061318158216, itd-2609051402083398): coordination with map #12's own article-eggs.js is by a DOM convention (the .egg-panel-backdrop class its panel and its opening modal both already carry) rather than by editing that file: while the class is present, article-keys.js does nothing but translate C-g into a synthetic Escape keydown, because article-eggs.js listens for Escape literally and never for C-g. The dispatched event carries a marker property (articleKeysRelayed) rather than being distinguished by event.isTrusted, because a real browser marks every script-dispatched event untrusted (which would have worked in production) but a test has no way to dispatch a trusted keydown at all, so the guard needed a property this script controls either way to be provable.
- 2026-09-06 — map #26 (spc-2609061318158216): article-keys.js adds one visible, native <button type="button"> ("Keyboard shortcuts"), first in <body>, that opens the keys panel — beyond the intent's own literal Acceptance Criteria — to answer itd-2609061324342715's forbid on a panel reachable only by an Emacs chord; the contents list and search were judged not to need an equivalent button, because each already has a fully accessible route with no chord at all (the on-page nav, and the browser's own native find).
- 2026-09-06 — map #26 (spc-2609061318158216): the reader-controls toolbar's own reachability by Tab is left exactly as article-controls.js (map #10) already built it — appended last in document.body, so Tab reaches it only after every article element — rather than moved or given a jump chord, because the design brief's own text forbids inventing a chord and moving the toolbar's DOM position is map #10's file to change; this map's own guarantee is only that its listener never calls preventDefault on Tab and never traps focus outside an open overlay, confirmed by manual check M26-3 rather than solved here.
- 2026-09-07 — Resolved the two-slug-rule departure DECISIONS.md previously recorded (Fable F16, review round one): src-tauri/src/new_document.rs's slugify now folds accented Latin letters to their plain ASCII form through a direct table, fold_accent, covering the Latin-1 Supplement and Latin Extended-A ranges — enough to agree with src/core/outline.ts's Unicode-NFKD-based slugify on every case its own test pins (café to cafe, Technikfolgenabschätzung to technikfolgenabschatzung) — rather than pulling in a Unicode normalisation crate AGENTS.md's no-dependency rule forbids; a Latin letter outside that table still falls through to a hyphen as before, which no test on either side exercises.
- 2026-09-07 — iss-2609070642209805 (Fable F1/A1): the once-only opening and the collected eggs are now scoped per document, not per origin. src/core/render/article-eggs.js derives a runtime scope — a data-document-scope attribute on <body> if the host wrote one, else location.pathname with any /v/<hash>/ segment stripped — and suffixes both localStorage keys with it, so a version of a document shares memory with its own stable link while a different document (or a different id/token pair on the same site) never does. The app's preview window never navigates between two documents it shows in one session, so its pathname alone cannot scope it; src/doctree.ts's PreviewSource gains an optional documentScope field (a hash of the document's own folder name, never the path itself — AGENTS.md forbids a local path in anything the page carries) and src/preview.ts writes it onto <body> before article-eggs.js boots. Populating that field from the shell (hashing the open document's own folder name in src/app.ts, or in src-tauri/src/preview.rs alongside DocumentRoot) is outside this agent's owned files (src/app.ts, src-tauri/*) and is left for whoever owns them; until then the app's own preview falls back to the same fixed-pathname scope it had before, which the site's own per-document scoping does not depend on.
- 2026-09-07 — iss-2609070642203845 (Fable F27): the article's own keyboard script no longer takes the browser's keys it never meant to answer. src/core/render/reading-keys.ts's readingBindingData drops s-f from isearch-forward's own chords (the spec's own Design already names the browser's native find as the accessible route) and Down/Up from next-line/previous-line (bare, they are the browser's own scroll; C-n/C-p still answer both), filtering a row's chords rather than the shared BINDINGS table so the editing surface keeps every one of its own. src/core/render/article-keys.js's onKeydown no longer calls preventDefault on a C-c/C-x prefix start while window.getSelection() reports a non-empty selection, since both are copy and cut on Windows and Linux Chromium; C-h, this page's other prefix, has no such conflict and is untouched.
- 2026-09-07 — Fable F6 (reader-controls toolbar at 390 CSS px): resolved by adding a single toggle button rather than moving the toolbar in flow at 760px and below, because it keeps the toolbar's own shape and its existing tests unchanged at every width and answers itd-2609051336128348's 'cannot be reached or dismissed' with one small, reversible state (aria-expanded) rather than a second layout mode. src/core/render/article-controls.js wraps the three <fieldset>s and the reset in a new .article-controls-body the toggle shows or hides via the hidden attribute, starting expanded so nothing about a first read changes; article.css moves the toolbar's own background/border onto that body and adds the toggle's own small, permanent-footprint styling.
- 2026-09-07 — Fable F13 (the .opening block's once= attribute was ignored): src/core/render/article.ts's renderOpening now emits data-once from block.attributes.pairs["once"], falling back to "per-browser" only when the author wrote no attribute at all, rather than hard-coding the string regardless of what was written. Chosen over amending the how-to page, because reading the attribute is the smaller and more honest fix and leaves docs/how-to-hide-things-in-the-text.md's own claim ("once=\"per-browser\" is the only form the article renders as a modal") true either way: article-eggs.js still keys the modal behaviour off the attribute's presence, not a particular value, since no second form is designed or documented yet.
- 2026-09-07 — GLM F8 (the preview window probed remote: video sources from the app): resolved by gating the probe rather than carving out an exception in 02-constraints.md's 'network only on publish', because the app touching the network on a plain Preview press has no publish behind it at all. src/preview.ts's show() now hands article-video.js's upgradeVideos a prober that resolves an external http(s) address to false without ever calling the real probe, and only forwards an already-local address (a data: URI readAssets already produced) to it; the published site's own preview (build.ts/articleDocument) is unaffected; a local/site source, already resolved to a data: URI before this point, still probes for real since nothing there touches the network either.
- 2026-09-07 — Fable F7 and GLM F4 (citations, itd-2609051335502171): src/core/bibliography.ts's resolveCitations/citationsIn gain an optional rendering parameter that skips a block, and everything inside it, wherever that rendering's own canon placement for it is absent — so a citation written only inside a .notes div earns no article entry or margin note, since nothing on the article page ever points at it, while an unfiltered call (the deck's own) still credits it, matching citationLinesFor's own documented intent to search a slide's speaker notes. src/preview.ts and src/publish/build.ts's own citationsFor now resolve the article's citations with rendering: "article" and the deck's own with no filter, as two separate calls sharing one bibliography parse rather than one shared resolution, because the article and the deck genuinely disagree about whether a .notes-only citation counts. The same two call sites now return EMPTY_RESOLUTION rather than undefined for the article when a document names no bibliography at all, so a citation is marked unresolved the same way an unknown key is rather than printed as the literal brackets html.ts's undefined fallback shows — that fallback is now reserved for a caller genuinely outside a resolution's reach, the deck's own inline rendering, which never reads the citations field of its RenderContext either way.
- 2026-09-07 — Resolved iss-2609070642208293 (B1, review round one Fable F24/Sonnet F2): src/app.ts's pickAndOpenSource now runs mayDiscard() between the pick and the call to openDocumentSource, rather than after it — openDocumentSource claims the nonce and moves the shell's own DocumentRoot and folder watcher the instant it is called, so running the discard guard after it left a declined discard with the old chapter still on screen while the shell was already confined to the new root. The nonce's own thirty-second lifetime covers however long the confirm dialog takes, matching the order app.openFolder already kept between choosing a folder and opening it; a spy-backed test in src/open-source.test.ts proves a refused discard calls openDocumentSource zero times.
- 2026-09-07 — Resolved iss-2609070642207436 (Fable F25, review round one): a document opened from a bare Markdown file (map #35, itd-2609061509393380) used to rebuild through a real whole-folder walk on every reload, because app.reload calls the same open_folder command a genuine folder open does and that command never remembered which shape it was asked for. src-tauri/src/open_source.rs gains SingleFileRoot, set by open_document_source to the bare file whenever resolve_source finds no real document above it; src-tauri/src/lib.rs's open_folder checks SingleFileRoot::matches before walking, and rebuilds through document::read_single_chapter for a root that matches instead. The watcher over a bare file is also narrowed to the file itself rather than the whole parent folder, on the same state, so a sibling changing — including the author's own save landing beside it — no longer fires a reload that would have shown the wrong thing even before this fix reached it. Chose the shell-remembers-the-shape design Fable's own finding named over the alternative (filtering watcher events by path alone) because reload can also be pressed directly, which a watcher-only fix would not reach.
- 2026-09-07 — Resolved iss-2609070642219301 (Fable F4 and F26, review round one): a document that declares no variants: at all — exactly the shape C-x C-n writes, and the shape a bare Markdown file opened on its own has, since read_metadata answers empty metadata for a folder with no document.yaml — used to be refused outright by documentForPublish (src/publish/services.ts), so the tutorial's own first dry run, and any publish or export of a document opened by C-x C-o, reached a dead end. variantFor now answers DEFAULT_VARIANT ("default") for a document that declares no variant at all, publishing and exporting it as its own single default variant with one build and no name the author was never asked to give; a document that declares one or more variants but names no default_variant: keeps the refusal unchanged, since guessing among several named candidates could publish one nobody confirmed. Needs no change on the Rust side: mint_identity's variant_tokens: map is already keyed by an arbitrary string, never validated against variants: when that list is empty. docs/tutorial-your-first-talk.md, docs/how-to-open-a-file-or-a-folder.md, docs/how-to-export-a-folder.md, docs/how-to-connect-the-production-repository.md and docs/how-to-start-a-new-document.md, and specs spc-2609061318158586 and spc-2609061617208646, now say so.
- 2026-09-07 — For B6 (review round one, Fable F15 and Sonnet F5, found beside iss-2609070642219301 rather than ledgered separately): src-tauri/src/new_document.rs::create_document used to call fs::create_dir unconditionally on the Part folder it writes into, 01-chapters, which fails with the OS's own "File exists" whenever that folder is already there and empty — a shape what_it_already_holds already treats as not yet a document, so the create should succeed rather than surface a raw filesystem error. Chose to write into the existing empty Part rather than Fable's other offered fix, picking the next free numbered prefix (the way document.rs's next_prefix does for a Chapter): a Part's own name is not renumbered elsewhere in this codebase once written, and 01-chapters is the one name this call ever gives its first Part, so there is only ever one to find already there. The pre-existing test named for this case seeded an unrelated empty folder (01-empty) and asserted the opposite of what production code already did (Sonnet F5); renamed to a_second_document_is_still_creatable_beside_an_empty_part and added writes_into_an_already_present_empty_first_part_rather_than_refusing for the real case.
- 2026-09-07 — Resolved iss-2609061520160056: src-tauri/src/watch.rs's two timing tests already polled with a deadline rather than a fixed sleep, but a longer deadline over the one filesystem operation each performed did not stop the flake, because the machine this ran on is shared with other concurrent agent work and the miss was not lateness but loss — a rename or a write performed right after watch() returns can race the OS's own event delivery under enough scheduling pressure, missed rather than merely reported late. Replaced wait_for with trigger_until_seen, which repeats a fresh filesystem operation roughly every quarter-second, well past the debouncer's own DEBOUNCE window, until the change is seen or up to thirty seconds pass (widened from an initial ten once even that still flaked on this machine, which fseventsd's own CPU use shows is itself contended, not merely this crate's tests), so a watch that was not yet truly listening for the first attempt is still caught by a later one. Verified with ten consecutive full-suite runs at default parallelism.
- 2026-09-07 — For B5 (review round one, Fable F12 and GLM F5): src/app.ts now keeps a per-chapter cursor offset, chapterCursors, recorded on the way out of a chapter in openChapter and restored on the way back in when no heading was named — the doc and itd-2609061318091323 AC8 promise a chapter opens "with the cursor where she last left it", which nothing implemented before. The map is cleared whenever the document root itself changes (openFolder, pickAndOpenSource) rather than only on close, since a relative chapter path can recur across two unrelated documents and a stale offset from one has no business landing in the other; it is deliberately kept across closing and reopening the same chapter within one document, which is the case the criterion is actually about.
- 2026-09-07 — For Fable F10 (review round one): a bibliography src/app.ts's readSidebarData cannot read used to be a silent console.warn while every citation key in every chapter was badged unresolved, as if each key itself were the fault. bibliographyError now carries the read failure separately from the parsed Bibliography, so no key is badged on its account, and each of openFolder, pickAndOpenSource and reload folds it into the announcement it already makes (the same suffix shape openFolder's own "entries unreadable" already takes) rather than a bare announce() call inside readSidebarData that a caller's own later announcement would immediately overwrite.
- 2026-09-07 — Fable F8 (two hand-kept article compositions): src/publish/build.ts exports composeArticle(entries, citations) — the contents list, every chapter's own page, and the reference list, joined — and both renderVariant and src/preview.ts's articleOf now call it, rather than each hand-building the same join with its own id-prefix rule. preview.ts's own RenderedChapter interface is replaced by build.ts's exported ComposedChapter, the shape both a synchronous (publish) and an asynchronous (app preview) chapter render produce. build.test.ts's own new test renders one DocumentSource through renderVariant and the same document through preview.ts's articleOf and asserts the composed fragment is identical.
- 2026-09-07 — Completed the wiring iss-2609070642209805's fix (Fable F1, per-document localStorage scope) left for src/main.ts/src/doctree.ts (shared hubs, this pass's own files): src/doctree.ts's PreviewSource.documentScope and src/preview.ts's applyDocumentScope existed with nothing yet computing a value for the app's own Preview command to send. Added documentScopeOf to doctree.ts, at the coordinator's own request over an earlier FNV-1a draft — the first sixteen hex characters of a SHA-256 digest of the open document's root, computed with crypto.subtle and never exported past the digest itself — and set it on the object main.ts's loadForPreview hands previewDocument, so two documents Alice previews in the same app session no longer share the once-only opening or the collected eggs. Placed in doctree.ts rather than left in main.ts (which runs its whole app bootstrap at import time and so has no test file) so src/doctree.test.ts could prove it directly: a lower-case hex digest that never contains the root's own text, stable for the same root, and distinct between two different ones. The published site's own preview (build.ts/articleDocument) is a separate path this does not touch.
- 2026-09-07 — Phase two closes: eight specs close in one change after the first review round's fixes landed (spc-2609061318090042, spc-2609061318154502, spc-2609061318151591, spc-2609061318159422, spc-2609061318158216, spc-2609061318158586, spc-2609061341298538, spc-2609061617208646), shipping maps #9, #10, #11, #12, #26, #29, #34 and #35; each owes a fidelity audit, ingested before the release is cut.
- 2026-09-07 — Fixed iss-2609070746140986 (fidelity audit of itd-2609051335502171, ac-4: a rule-opened slide's face printed [@smith2020] and [@nosuchkey] literally): src/core/render/slides.ts's renderSlides gained a fourth citations parameter, threaded into contextFor so the headline and the face read the deck's own resolution — the same one deck.ts's citationLinesFor already reads for the foot's credit line — while renderSlide strips it back to undefined for the speaker's own notes, which 04-surfaces.md section 5 still calls not a reader's surface. publish/build.ts's citationsFor now always returns EMPTY_RESOLUTION for the deck rather than undefined when a document names no bibliography, the same 'ordinary document, not an error' shape the article already had (GLM F4), and hands it to renderSlides too; present.ts resolves the chapter's citations the same way through doctree.ts's readBibliography, with no rendering filter, so the deck at the lectern and the deck at the link agree. Departs from spc-2609061318151591's own Design section ('the deck names a cited work through a credit line … not through this inline marker'), amended in place rather than superseded, since the audit found the original reasoning true of the foot but not of a Section's own prose reaching the face through a rule's continuation.
